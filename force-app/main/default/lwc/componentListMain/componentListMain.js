import { LightningElement } from 'lwc';


export default class ComponentList_main extends LightningElement {

    renderedCallback() {
    }

    handleFilterChange(event) {
    }

    handleRowAction(event) {
    }

    handleTileAction(event) {
    }

    handleModalAction(event) {
    }
}